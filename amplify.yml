version: 1
frontend:
  phases:
    preBuild:
      commands:
        - "npm ci --cache .npm --prefer-offline"
    build:
      commands:
        - "npm run build --workspace=apps/www"
  artifacts:
    baseDirectory: apps/www/out
    files:
      - "**/*"
  cache:
    paths:
      - ".npm/**/*"
